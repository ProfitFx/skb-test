С помощью любой системы сборки (желательно Maven) и на любом языке (C#) написать необходимый набор тестов.

Проверка:
Из консоли Windows в папке с вашим исполняемым файлом выполним команду сборки (mvn install || mnv test и т.п.).

Тесты надо писать с расчётом на других тестировщиков, которые будут в дальнейшем добавлять проверки и дописывать кейсы. 
Код должен соответствовать вашим критериям чистого кода и должен быть рассчитан на длительную поддержку.

Что надо протестировать:
# Отправку сообщения ORDERS через АПИ.
# Наличие ошибки при повторной отправке сообщения без изменений.
# В интерфейсе поставщика проверить наличие отправленного сообщения.
# В карточке сообщения на вкладке "Заявка" проверить: 
## Корректность названия товара №2 и №4 (названия можно найти и менять из файла ORDERS).
## Скачать xlsx и проверить корректность названия товара №2 и №4 в скачанном файле.

Необходимая информация для тестирования:
# Описание АПИ https://test-edi-api.kontur.ru/Documentation/V1/Index.html
Методы для отправки сообщения: GetBoxesInfo, SendMessage
# Ключ разработчика: profitfx-e1d4b253-79d2-41c2-9304-be97d6bb48cd
# Во вложении есть файл ORDERS, пригодный к однократной отправке. Для повторной отправки надо инкрементировать значение NN01 на 1.
# Интерфейс поставщика: https://test-edi.kontur.ru/25a6dc25-4796-45b8-81bd-c43a7ccac371/Supplier 
# Логин profitfx@mail.ru, пароль - получить через форму восстановления.

Вопросы по заданию или приложению задавайте через Лысцеву Надежду.